# Roadmap

The core product is built: a single-player editor, content systems (uploads, stock, templates), accounts and workspaces, document types (presentations, video, whiteboard, docs, sheets), export, brand kits, a bring-your-own-key AI layer, and the AI Creative Studio (agentic outline-first multi-page generation, a conversational design assistant, Magic transforms, and brand/style grounding) all ship today on the Go + Next.js stack described in `README.md`.

Specs for already-shipped features are archived in [`../shipped/`](../shipped/) (the shipped code is the source of truth for those). What stays below is the work that is genuinely unbuilt or early, kept as a forward-looking spec for each.

For the product-wide north star (the goals and differentiators we hold ourselves to), see [requirements.md](requirements.md).

## Remaining work

| Area | Spec | State |
| --- | --- | --- |
| Realtime collaboration (CRDT, presence, offline sync, branch/restore) | [16-realtime-collaboration.md](16-realtime-collaboration.md) | Core + multi-instance shipped: Yjs CRDT sync over `/realtime`, presence/cursors/follow, offline (IndexedDB), authed + read-only enforced, collaborative locks with heartbeat-TTL release, per-user undo, char-level text merge, live permission downgrade, horizontal Redis fan-out + cross-instance roster catchup + Redis-CAS lock authority, CRDT history scrubber + restore, automatic snapshots, update-log compaction. Remaining: per-page subdocuments + lazy load at scale (+ GPU-paint AC-10 proof), true in-CRDT branches, server-authoritative last-leave snapshot, and on-the-wire per-node enforcement (needs a server-side CRDT decoder; deferred). |
| AI media (captions, TTS, music, avatar, lip-sync, image-to-video) | [23-ai-media.md](23-ai-media.md) | Not started: blocked on the video media pipeline and audio/video model endpoints. |
| Presentations (beat PowerPoint/Keynote/Slides/Gamma/Pitch: PPTX round-trip, masters/themes, presenter view, present-and-record, live audience, AI decks, charts, a11y) | [28-presentations.md](28-presentations.md) | Core shipped: presentations as the multi-page editor (`Page` present fields + the F25 `NodeAnimation`/`KeyframeTrack`/`Interaction`/`ImageMotion`/`PageTransition` model, the pure `@hc/engine` `animation.ts`/`pose.ts` playback core that renders identically in editor preview, present mode, and animated export, the `PresentMode.tsx` runtime with 9 transitions incl. id/name-matched Magic Move morph + laser/pen/spotlight + autopilot + a presenter HUD with rehearsal timer, F39 AI prompt-to-deck, charts/tables, sharing + per-page engagement insights, image/PDF/SVG/APNG/GIF export). Remaining: PPTX import/export round-trip, slide masters/layouts/themes + sections, a true second-display presenter view, present-and-record + full-deck video, live audience Q&A/polls/reactions/captions, AI design autopilot + whole-deck translation + speaker notes + doc/URL/file ingestion, live data-linked charts + bulk merge, accessibility leadership (alt text, reading order, checker, tagged PDF, reduced motion), and 60fps present at scale. |
| Whiteboard (beat Miro/FigJam: infinite canvas, ink, AI canvas, facilitation, diagram-as-code, scale, a11y) | [30-whiteboard.md](30-whiteboard.md) | Core shipped: whiteboard document type (`@hc/whiteboard` routing/layout/templates/sessions, the `WhiteboardSurface` over the shared `Canvas`, sticky/connector/frame schema nodes, F16 realtime + presence/reactions/cursor-chat, comments, sharing, dot-voting, timer, convert-to-deck). Remaining: true infinite canvas, board-native ink + free-draw connectors, the full facilitation suite (private mode, spotlight/take-control, server-authoritative voting, breakouts), AI canvas agents, diagram-as-code round-trip, performance at 10k+ objects, and accessibility leadership. |
| Accessibility, i18n, security, compliance, self-host, NFR | [38-accessibility-i18n-security-compliance-selfhost-nfr.md](38-accessibility-i18n-security-compliance-selfhost-nfr.md) | Partial: the design accessibility checker ships, and the self-host baseline is strong (single self-daemonizing binary, browser/CLI first-run setup wizard with reverse-proxy support, `storage migrate` to S3, per-workspace and per-user storage quotas with UI meters, multi-platform release binaries plus lean channel-tagged Docker images cut from `stable` only, OIDC SSO, MFA). Remaining: i18n, SCIM, observability beyond structured logs, compliance programs, Helm, and the enterprise controls (audit log, DLP, CMEK, residency). |

Each spec follows the original 15-section template (context, requirements, data model, API, acceptance criteria, tests). Read the spec before picking up its area, and keep it in sync if scope changes.

## Editor parity backlog

The editor reached Canva parity on every audited gap: all 56 tracked items ship, and the one deliberately out-of-scope item (semantic/embedding element search) is not planned. The completed, code-audited record is archived at [`../shipped/editor-parity-backlog.md`](../shipped/editor-parity-backlog.md). Start a fresh backlog if new Canva-parity gaps surface.
