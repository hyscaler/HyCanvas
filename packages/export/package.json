{
  "name": "@hc/export",
  "version": "0.1.0",
  "license": "Elastic-2.0",
  "private": true,
  "description": "HyCanvas export engine: page selection, raster sizing, filename templating, presets, pre-flight, and editable SVG (framework-agnostic)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hc/schema": "*",
    "@hc/engine": "*",
    "@hc/geometry": "*",
    "@hc/color": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.6"
  }
}
