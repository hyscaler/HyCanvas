{
  "name": "@hc/geometry",
  "version": "0.1.0",
  "license": "Elastic-2.0",
  "private": true,
  "description": "HyCanvas geometry: shape-to-path, bounds, hit-testing, boolean ops, connector routing (framework-agnostic)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hc/schema": "*",
    "polygon-clipping": "^0.15.7"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.6"
  }
}
