{
  "name": "@hc/sdk",
  "version": "0.1.0",
  "license": "Elastic-2.0",
  "private": true,
  "description": "HyCanvas typed REST/WS client used by the web app and the public API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hc/authz": "*",
    "@hc/brandkit": "*",
    "@hc/schema": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
